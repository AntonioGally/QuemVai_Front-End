import styled from "styled-components";

export const MyTitleForm = styled.h3`
  margin-top: 2%;
  font-family: "Poppins";
  color: var(--fontWhite);
  font-size: 40px;
  @media (max-width: 768px) {
    font-size: 20px;
  }
`;
